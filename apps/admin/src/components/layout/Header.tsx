import { GridItem } from '@chakra-ui/react';
import { Heading } from '@chakra-ui/react';

export const Header = () => {
  return (
    <GridItem area={'header'} display={'flex'} justifyContent={'center'} alignItems={'center'}>
      
    </GridItem>
  );
};
